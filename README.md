# GraphQL_Agent
Automatic graphqli tool that detects the vulnerabilities in graphql applications.

Enter the url of the applcation, it will search for the graphql endpoints and find common vulnerabilities. It will store output of vulnerabilities like introspection on local machine in a json format.
